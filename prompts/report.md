# Report prompt

Use only the sources in this notebook. Do not invent books, data, quotes, or page numbers.

Write a structured briefing with these headings, in this order. Keep prose compact: short paragraphs and bullets, not a chapter. Do not pad. Do not collapse distinct named rules into a few vague themes to save space.

1. Summary — what the sources are about, in plain language (one short section). If a source is a story, sketch the plot only enough to make the rules intelligible.
2. Key claims — each with the source title (and author if known).
3. Agreements and disagreements across sources. If there is only one source, say so in one line and skip the rest of this section.
4. Rules and action plan — the main section. Numbered list. One item per distinct rule or habit the sources name. For each: the name the source uses, one sentence on what it says to do, then title (and author if known). If the sources give an order (first / next / then), keep that order. Do not invent a 1-2-3 sequence, extra steps, products, tickers, or allocations. This is the sources’ checklist, not personal advice.

   After you have the full list: if it has more than 5–7 items, do not leave one long run. Split it into modules of 5–7 items each. Use module headings the sources already name (phases, chapters, named groups). Keep every distinct rule; do not merge items or collapse them into themes to hit the cap. If the sources do not name groups, cluster only adjacent items that belong together in the source, and label those clusters as grouping labels, not as extra steps.
5. Open questions and limits of the sources.
6. Quotes — only when a short attributed line is needed to avoid misstating a claim.

Style: clear headings, no filler, no “as an AI” framing.

If a point is not in the sources, omit it or label it as an inference and say why.
